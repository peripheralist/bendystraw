# BendyStraw

BendyStraw enables querying Juicebox subgraph data from multiple blockchains in a single graphql query.

## Dev

`yarn codegen` downloads graphql schema from a chosen subgraph url, then uses the schema to generate types.